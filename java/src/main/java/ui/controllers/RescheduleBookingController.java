package ui.controllers;

import ui.container.AppContainer;
import ui.container.Base;
import ui.container.InjectableObject;

public class RescheduleBookingController extends InjectableObject {
	/**
	 * Constructs a controller.
	 * @param _appContainer		The appContainer.
	 * @param parent			The parent of the controller.
	 */
	public RescheduleBookingController(AppContainer _appContainer, Base parent) {
		super(_appContainer, parent);
	}
	
	public void attach(
			) {
		
	}
}
